name: Drip Email Cron

on:
  schedule:
    - cron: "15 * * * *"   # hourly at :15 UTC
  workflow_dispatch:

jobs:
  send-drips:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Send drip emails via Resend (bypass Vercel/Cloudflare 1010)
        run: python scripts/send_drips.py
